export interface WorkoutStreak {
  streak: number;
  lastWorkoutDate: string | null;
}

export interface WorkoutCounts {
  daily: number;
  weekly: number;
  monthly: number;
  lastWorkoutDate: string | null;
}


